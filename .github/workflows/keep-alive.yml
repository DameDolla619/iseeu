name: Keep IseeU site awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the live site
        run: curl -sf https://iseeu.onrender.com/ -o /dev/null -w "Status: %{http_code}, took %{time_total}s\n"
